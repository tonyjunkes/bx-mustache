/**
 * bx-mustache module configuration.
 */
class {
	property name="moduleRecord";
	property name="boxRuntime";
	property name="log";

	this.version = "1.0.0";
	this.mapping = "bxMustache";
	this.author = "Tony Junkes";
	this.description = "Mustache syntax template rendering for BoxLang.";
	this.webURL = "https://github.com/tonyjunkes/bx-mustache";
	this.enabled = true;
	this.dependencies = [];

	function configure() {
		// These defaults can be overridden by the bx-mustache module entry in boxlang.json.
		settings = {
			cacheEnabled: true,
			cacheMaxEntries: 200
		};
	}
}
